services:
  - type: web
    name: hex99
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app -b 0.0.0.0:10000
    envVars:
      - key: PORT
        value: "10000"
